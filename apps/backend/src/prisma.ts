/**
 * DEPRECATED: Use prisma from '../db' instead
 * This file is kept for backwards compatibility only
 * All new code should import from '../db'
 */

import { prisma } from './db';

export default prisma;
